import React from "react";

const Phonetics = ({ audio, text}) => {

  return <div className="hero-header">
    <div className="hero-header-text"> {text} </div>
  </div>;
};

export default Phonetics;
